.read Edge(s, d) from "./inputs/graph_edges.csv" as "csv".

.iterate {
    Path(x, y) :- Edge(x, y).
    Path(x, z) :- Path(x, y), Edge(y, z), x != z.
}

Degree(node, count(edge)) :- Edge(node, edge).


.write Degree to "/tmp/degrees.csv" as "csv_with_header".